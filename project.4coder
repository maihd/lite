version(1);

project_name = "lite";

patterns = {
    "*.c",
    "*.h",
    "*.bat",
    "*.4coder",
    ".clang-format"
};

blacklist_patterns = {
    ".*"
};

load_paths = {
    { { { "./src", .relative = true, .recursive = true, } }, .os = "win" },
};

command_list = {
    
};

